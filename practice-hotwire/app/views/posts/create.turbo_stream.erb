<% if @post.errors.any? %>
	<%= turbo_stream.update Post.new, partial: "posts/form", locals: { post: @post } %>
<% else %>
	<%# turbo_stream.prepend "all_posts", partial: "posts/post", locals: { post: @post } %>
	<%= turbo_stream.prepend "all_posts", @post %>
	<%= turbo_stream.update Post.new, "" %>
	<%# @post.to_partial_path %>
<% end %>
