<div id="<%= dom_id post %>">
	<p>
		<strong>Title:</strong>
		<%= post.title %>
	</p>
	<p>
		<strong>Slug:</strong>
		<%= post.slug %>
	</p>
	<p>
		<strong>Body:</strong>
		<%= post.body %>
	</p>
	<div>
		<%= link_to 'stats', post_stats_path(post) %>
	</div>
</div>
